# Clean Touch — version fonctionnelle

## Installation
```bash
npm install
npm run dev
```

## Build
```bash
npm run build
npm run preview
```

Cette version n'utilise pas Tailwind.
Le CSS est entièrement inclus dans `src/styles.css`, donc tout fonctionne directement après installation.
